---
description: chrome-live-session：扩展桥操作用户 Chrome；real-browser MCP
alwaysApply: true
---

# Chrome Live Session

| 叫法 | 含义 |
|------|------|
| Skill `chrome-live-session` | 调用流程见 `.cursor/skills/chrome-live-session/SKILL.md` |
| MCP `real-browser` | `.cursor/mcp.json` 中配置；Cursor 里可能显示为 `project-*-real-browser` |

**流程**：`list` → `focus` → `snapshot`/`browser_text`；点菜单必须 `click`，不能只读文本。

**工程细节**（改扩展/端口）：参考姊妹仓 `D:\zr\学习\cursor-learning\.cursor\rules\chrome-live-session.mdc` 或 [real-browser-mcp](https://github.com/ofershap/real-browser-mcp)。
