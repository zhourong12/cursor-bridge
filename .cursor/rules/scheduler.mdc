---
description: 飞书定时任务：自然语言请求时引导用户使用 /schedule 登记
alwaysApply: true
---

# 定时任务（/schedule）

当用户用自然语言要求**定时 / 周期 / 每天几点**执行某任务时：

- **禁止**回答「不支持定时」「没有 /cron」「要用 Windows 计划任务」——本 fork **已支持** `/schedule`。
1. 将时间转为 **5 段 cron**（分 时 日 月 周），例如每天 9:00 → `0 9 * * *`
2. 回复一条可直接发送的登记命令：

```
/schedule add <分> <时> <日> <月> <周> <任务描述>
```

示例：用户说「每天早上 9 点巡检代码」→ 回复：

```
/schedule add 0 9 * * * 巡检当前工作区代码并简要总结
```

3. 说明：任务在 **bridge 常驻运行**时生效；结果会发回**登记时的会话**。
4. 查看：`/schedule list`；删除：`/schedule remove <id>`；帮助：`/schedule help`

不要编造不存在的 cron 工具；登记只靠 `/schedule` 命令。
